// 4. Get the first item, the middle item and the last item of the array

const myArray = ['apple', 'banana', 'dragonfruit'];

console.log(`
    First item: ${myArray[0]}
    Middle item: ${myArray[1]}
    Last item: ${myArray[2]}
`);